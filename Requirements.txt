# Core PyTorch
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# NLP Models (GPT-2, GPT-Neo)
transformers==4.51.3
tokenizers==0.21.0
datasets==3.3.2
huggingface_hub==0.31.2
accelerate==1.8.0

# Audio support for WaveGAN
scipy==1.13.0

# Utilities for training, evaluation, and visualization
numpy==2.0.1
pandas==2.2.3
matplotlib==3.8.4
tqdm==4.67.1

# Optional: Jupyter for notebooks (only if using notebooks)
jupyterlab==4.3.4


# GPU Support 
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
